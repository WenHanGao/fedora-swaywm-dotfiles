# Device-specific input rules belong here. Discover identifiers with:
# swaymsg -t get_inputs
#
# input "2:14:SynPS/2_Synaptics_TouchPad" {
#     dwt enabled
#     natural_scroll enabled
# }

# Lock immediately when the laptop lid closes.
bindswitch lid:on exec ~/.config/gtklock/lock --daemonize
